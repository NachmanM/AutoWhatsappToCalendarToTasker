# WAHA Connection Settings
# ------------------------
# NOTE: If running inside Docker, 'localhost' refers to the container itself.
# If WAHA is in a different container, use 'http://host.docker.internal:3000' 
# or your machine's LAN IP (e.g., http://192.168.1.50:3000).
WAHA_BASE_URL=http://waha:3000

# The session name you used in WAHA (usually 'default')
WAHA_SESSION=default

# The specific WhatsApp Chat ID to listen to (ends in @g.us or @c.us)
WAHA_CHAT_ID=1234567890@g.us

# Must match the key set in the WAHA .env (if any)
WAHA_API_KEY=your_secret_api_key_here

# AWS Credentials (For S3 Upload)
# -------------------------------
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=us-east-1
AWS_BUCKET_NAME=your-s3-bucket-name

# Google Gemini AI (For Image Analysis)
# -------------------------------------
GEMINI_API_KEY=your_gemini_api_key
